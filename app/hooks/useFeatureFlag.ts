/**
 * Placeholder feature flag hook. Always returns false.
 * Replace with a real implementation when available.
 */
export function useFeatureFlag(_flag: string): boolean {
  return false;
}

export default useFeatureFlag;
